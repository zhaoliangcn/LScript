#scp

加载:扩展对象,socket

定义:地址,服务器地址,192.168.43.58,8888

定义:套接字,服务套接字

定义:套接字,连接套接字

定义:字符串,消息

如果(命令行参数.取大小()==3)
	定义:字符串,ipaddr
	ipaddr =命令行参数[2]
	服务器地址.设置(ip,(命令行参数[2]))
结束

服务器地址.显示()

服务套接字.创建()

服务套接字.绑定(服务器地址)

服务套接字.侦听()

服务套接字.接受(连接套接字)

连接套接字.接收(消息)

消息.显示()

连接套接字.发送(消息)

连接套接字.关闭()

服务套接字.关闭()
